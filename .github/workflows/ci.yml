name: CI

on:
  workflow_dispatch:
  pull_request:
    branches: [main]

# Uncomment to enable CI on direct push to main
# on:
#   push:
#     branches: [main]

env:
  TF_VERSION: "1.7.5"
  AWS_ACCESS_KEY_ID: test
  AWS_SECRET_ACCESS_KEY: test
  AWS_DEFAULT_REGION: us-east-1

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
      - run: terraform fmt -check -recursive

  validate:
    name: Validate
    runs-on: ubuntu-latest
    needs: fmt
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
      - name: Init (dry run)
        run: terraform init -backend=false
      - name: Validate
        run: terraform validate
      - uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: latest
      - name: TFLint
        run: tflint --init && tflint --format compact

  plan-dry-run:
    name: Plan (dry run)
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
      - name: Init (dry run)
        run: terraform init -backend=false
      - name: Plan (dry run)
        run: terraform plan -var-file=example.tfvars

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: .
          format: table
          severity: CRITICAL,HIGH
          exit-code: "1"

# ── COMMENTED OUT — activate once AWS OIDC role is configured ─────────────────
#
# plan:
#   name: Plan (real)
#   runs-on: ubuntu-latest
#   needs: validate
#   if: github.event_name == 'pull_request'
#   permissions:
#     id-token: write
#     contents: read
#   steps:
#     - uses: actions/checkout@v4
#     - uses: hashicorp/setup-terraform@v3
#       with:
#         terraform_version: ${{ env.TF_VERSION }}
#     - uses: aws-actions/configure-aws-credentials@v4
#       with:
#         role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
#         aws-region: us-east-1
#     - run: |
#         terraform init \
#           -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
#           -backend-config="key=network/vpn/terraform.tfstate" \
#           -backend-config="region=us-east-1" \
#           -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}"
#     - run: terraform plan -var-file=envs/dev.tfvars
